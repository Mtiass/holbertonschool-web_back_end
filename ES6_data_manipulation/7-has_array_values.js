// Function hasValuesFromArray, returns a boolean if all the array elements exist within the set.
export default function hasValuesFromArray(set, array) {
    return (array.every(val => set.has(value)));
}
